$schema: "http://json-schema.org/draft-07/schema#"
type: object
required: [files, collection]
properties:
  files:
    type: array
    items:
      type: string
  collection:
    type: string
    enum: [frameworks, industry, case-studies, interviews, workspace-specific, misc]
