$schema: "http://json-schema.org/draft-07/schema#"
type: object
required:
  - workspace_id
properties:
  workspace_id:
    type: string
    description: "Target workspace ID"
  source_ids:
    type: array
    items:
      type: string
    description: "Specific review source IDs to mine (optional)"
