$schema: "http://json-schema.org/draft-07/schema#"
type: object
properties:
  positioning_path:
    type: string
    description: "Path to the generated positioning statement"
  evidence_strength:
    type: string
    enum: [weak, moderate, strong]
    description: "Overall evidence strength supporting the positioning"
